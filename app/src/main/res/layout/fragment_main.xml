<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable
            name="viewModel"
            type="com.diegoferreiracaetano.chuckNorris.ui.main.MainViewModel"/>
        <variable
            name="eventCallbacks"
            type="com.diegoferreiracaetano.chuckNorris.ui.main.adapter.CategoriesListAdapter.Callbacks" />
    </data>
    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:onRefreshListener="@{() -> viewModel.getCategories()}"
        app:refreshing="@{viewModel.loading}">
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/list_categories"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                app:layout_behavior="@string/appbar_scrolling_view_behavior"
                app:eventCallbacks="@{eventCallbacks}"
                app:setReviewAdapter="@{viewModel.categories}"
                app:showLongMessage="@{viewModel.error}"
                tools:listitem="@layout/list_item_categories" />
    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>
</layout>
